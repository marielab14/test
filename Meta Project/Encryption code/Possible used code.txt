Possible way of connectting database and login page:



possible code for password encryption:

MessageDigest digest = MessageDigest.getInstance("SHA-256");
byte[] hash = digest.digest(password.getBytes(StandardCharsets.UTF_8));
Server.write(new String(hash, StandardCharsets.UTF_8);

MessageDigest digest = MessageDigest.getInstance("SHA-256");
byte[] hash = digest.digest(password.getBytes(StandardCharsets.UTF_8));
passwordHash = Server.readLine();
if((new String(hash, StandardCharsets.UTF_8)).equals(passwordHash)) authentication.success();

- openssl passwd -1 -stdin <<< password_here
OR
- # echo -n admin123 | makepasswd --crypt-md5 --clearfrom -
admin123     $1$ZUNNSLzQ$XsViFC1bhucsr3f8AzMPt/
OR
- ~  makepasswd --crypt-md5 --clearfrom file
admin123   $1$iQd/ujH.$rMXZiYwQC1Rc/rgO3.FeX/  